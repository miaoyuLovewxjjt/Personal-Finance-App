所有UI都严格遵守：同类型使用相同的UI图标，一样等级的内容使用一样大小的文字，保持严格的对齐和工整，遵守中心、上下对齐，不省略任何文字，换行内容也要保持对齐。
1、首先主页为基础页面，可以新建 账本，新建后可以在这个账本进行如下功能
  1）存款明细，支持自定义入库时间、类型、物品名称、备注、价值等，自动计算总存款，包含以下部分的价值
  a.金钱类
  b.非金钱类录入
  以上内容允许编辑、删除、自动按照类型、时间排列  2）记账明细：
  a.左侧为导航栏，左侧为 年 月 日多级展开，最下方为+号。
  b.添加账本后默认初始为今天
  c.点击加号后，会进入新的一页记账页面，新页面包含日期，在最上方，包含天气的选择，比如今天是什么天气，
    然后是：收支总览：结合图标列出今日总收入，并且可以直观地看出每个收入/花销类型所占比例
    分为 收入 和 支出，收入允许自定义今日的任何收入，支出允许记录今日的任何收入：
         收入：允许自定义收入时间，收入类型，收入金额，具体收入名称，收入备注
         支出：允许自定义花销时间，花销类型，花销金额，具体花销名称，收入备注
         以上内容允许编辑、删除、自动按照类型、时间排列
   d.月和年标题有下拉窗，默认收缩。
   e.同时年和月标题本身也可以点击进入，为年度/月度的花销总结，并且月度总结添加一个功能叫做一键同步按钮，点击后可以自动将该月攒的钱添加到存款明细，那边会添加一个内容就是9000元，攒钱，2026.9月收入已归档xxxx元这样的
     年总结内容包含：
     i1.年度总花销，总支出，哪个月支出最多，哪个月花销最多，此处月份添加跳转，可以跳转到该月
     i2.列图表，所有的花销，比如餐饮占了多少，交通用了多少，可以是一个圆形图，然后点击某个比例，可以展示具体的金额
     月总结内容包含：
     i1.月度总花销，总支出，哪天支出最多，哪天花销最多
     i2.列图表，所有的花销，比如餐饮占了多少，交通用了多少，可以是一个圆形图，然后点击某个比例，可以展示具体的金额
     以上内容为每天自动同步生成，不可编辑











# 像素记账 App · 完整提示词集

> 风格基调：星露谷物语式低分辨率像素马赛克（pixel art）
> 结构基调：主流记账软件（流水列表 + 底部录入面板）
> 适用工具：App 生成类 AI / 图像生成类 AI 均可分段使用

---

## 〇、全局 UI 硬性规范（所有页面必须遵守）

```
以下规范适用于每一个页面，任何页面都不得违反：
1. 同类型元素使用完全相同的 UI 图标，图标风格全局统一（均为像素马赛克、32x32 像素画）。
2. 同一等级的内容使用相同大小的文字；等级高低决定字号大小，不得同级不同号。
3. 保持严格对齐与工整：页面元素遵守中心对齐、上下对齐、左右对齐。
4. 不省略任何文字；需要换行的内容，换行后仍保持对齐。
5. 所有边框为深棕色像素描边，所有按钮/卡片带阶梯状硬像素阴影。
6. 字体为像素字体，颜色低饱和暖色（奶油白、草绿、木棕、暖黄、陶土橘）。
7. 背景为奶油米色像素纹理，无手绘平滑线条、无渐变渲染、无卡通吉祥物。
```

---

## 一、项目总览主提示词（喂给全能型 AI / App Builder）

```
请生成一款像素风格记账 App，完整需求如下：

【产品定位】
面向个人用户的记账软件，功能结构参考主流记账软件（随手记、鲨鱼记账、钱迹），
视觉风格采用星露谷物语式低分辨率像素马赛克（pixel art）：
马赛克颗粒清晰、色块感强烈、像素字体、阶梯硬阴影、深棕像素描边，无卡通吉祥物。

【页面清单】
1. 主页（账本管理）
2. 存款明细页
3. 记账明细页（左侧年月日导航）
4. 记账页（新增一笔）
5. 月度总结页
6. 年度总结页

【功能需求】
1. 主页：展示账本列表，可新建账本；新建后进入该账本。
2. 存款明细：支持自定义入库时间、类型（金钱类/非金钱类）、物品名称、备注、价值，
   自动计算总存款；内容可编辑、可删除，自动按类型、时间排列。
3. 记账明细：
   a. 左侧为导航栏，按 年→月→日 多级展开，最下方为「+」号按钮；
   b. 新建账本后默认定位到今天；
   c. 点击「+」进入记账页；
   d. 月和年标题带下拉窗，默认收缩。
4. 记账页（新增一笔）：
   日期显示在最上方，包含天气选择（如晴/多云/雨等）；
   然后是收支总览：结合图标列出今日总收入，并可直观看到每个收入/花销类型所占比例；
   分为「收入」「支出」两部分：
   - 收入：自定义收入时间、收入类型、收入金额、具体收入名称、收入备注；
   - 支出：自定义花销时间、花销类型、花销金额、具体花销名称、花销备注；
   以上内容均可编辑、删除，自动按类型、时间排列。
5. 月度总结页：
   显示月度总花销、总支出、哪天支出最多、哪天花销最多；
   圆形占比图展示各类花销比例（如餐饮占多少、交通占多少），点击某个比例展示具体金额；
   提供「一键同步」按钮：点击后自动将当月攒的钱归档到存款明细，
   生成一条记录如「9000 元，攒钱，2026.9 月收入已归档 9000 元」；
   本页数据每日自动同步生成，不可编辑。
6. 年度总结页：
   显示年度总花销、总支出、哪个月支出最多、哪个月花销最多；
   月份可点击跳转到对应月份的月度总结；
   圆形占比图展示全年各类花销比例，点击某个比例展示具体金额；
   本页数据每日自动同步生成，不可编辑。

【全局 UI 规范】
1. 同类型元素使用完全相同的 UI 图标，风格全局统一（像素马赛克）。
2. 同一等级内容使用相同大小文字；等级决定字号，不得同级不同号。
3. 严格对齐与工整：中心对齐、上下对齐、左右对齐。
4. 不省略任何文字，换行内容保持对齐。
5. 深棕像素描边、阶梯硬阴影、像素字体，低饱和暖色（奶油白、草绿、木棕、暖黄、陶土橘）。
6. 背景奶油米色像素纹理，无卡通吉祥物、无手绘平滑线条、无渐变渲染。

【交付要求】
产出可交互的网页应用，六个页面可正常跳转，全局视觉统一。
```

---

## 二、主页（账本管理）提示词

```
竖版手机记账 App 主页，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面顶部：居中是像素大字标题「我的账本」，下方一行像素小字「共 N 个账本」。
页面主体：账本列表，每本账本是一张像素卡片，卡片左侧为像素画账本小图标
（不同账本用不同颜色封面），中间为账本名称（如「日常账本」），
下方小字显示总存款金额（如「总存款 ¥12,345.00」），卡片右侧为像素箭头。
列表下方：一个居中的像素按钮「＋ 新建账本」，草绿色填充带硬阴影。

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 三、存款明细页提示词

```
竖版手机记账 App 存款明细页，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面顶部：居中是像素大字标题「存款明细」，下方一行像素小字显示自动计算的
「总存款 ¥XX,XXX.00」，用像素金币图标点缀。
页面主体：存款记录列表，每条记录一行像素卡片，字段从左到右依次为：
入库时间（如「2026-09-01」）、类型标签（「金钱类」橙色像素块 /「非金钱类」蓝色像素块）、
物品名称（如「现金」「黄金」）、备注、价值（如「¥9,000.00」），
右侧有「编辑」「删除」两个像素小按钮。
列表按「类型 → 时间」自动排序：先按类型分组，组内按时间从新到旧。
页面底部：一个像素按钮「＋ 新增存款」，草绿色填充带硬阴影。

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 四、记账明细页（左侧年月日导航）提示词

```
竖版手机记账 App 记账明细页，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面布局：左侧为竖向导航栏，右侧为流水列表。
左侧导航栏（约占 1/4 宽度）：按「年 → 月 → 日」多级展开，每级为像素小标签，
如「2026」下方展开「8月」「9月」，选中某月后展开具体日期；
导航栏最下方固定一个像素「+」号按钮，陶土橘填充，点击进入记账页。
右侧流水列表：默认显示今天（如标题「2026年8月27日 周四」），
下方按时间列出当天收支记录，每条一行像素卡片：
左侧像素小图标（餐饮汉堡/交通小汽车/购物袋/工资钞票等），
中间分类名 + 灰色像素小字备注，右侧金额（支出深棕 -¥43.00，收入草绿 +¥8,000.00），
每条按时间从新到旧排列。
月和年标题旁带下拉箭头，默认收缩展开的子级。

收入和支出分开两个标题来展示会更清晰

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 五、记账页（新增一笔）提示词

```
竖版手机记账 App「记一笔」页面，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面从上到下结构：
1. 日期：最上方居中是像素文字日期「2026年8月27日 周四」。
2. 天气选择：日期下方一行像素天气图标供选择（晴/多云/雨/雪等），
   每个图标为像素画，选中项带高亮像素边框，当前默认选中「晴」。
3. 收支总览：一个像素卡片，左侧列出「今日收入 ¥XX.XX」，
   右侧用像素小图标 + 数字列出各收入/花销类型所占比例（如餐饮 30%、交通 20%），
   配一个像素环形图直观展示占比，环用草绿/陶土橘/天蓝等区分。
4. 收入录入区：标题「收入」，字段依次为：收入时间、收入类型（下拉像素菜单）、
   收入金额、具体收入名称、收入备注，每个字段前有像素小标签。
5. 支出录入区：标题「支出」，字段依次为：花销时间、花销类型（下拉像素菜单）、
   花销金额、具体花销名称、花销备注。
6. 页面底部：一行像素操作栏，左侧「取消」按钮、右侧「保存」按钮（陶土橘填充）。

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 六、月度总结页提示词

```
竖版手机记账 App 月度总结页，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面顶部：居中是像素大字标题「2026年9月 月度总结」，标题旁带下拉箭头（默认收缩）。
标题下方：一个像素「一键同步」按钮（草绿色，带硬阴影），
点击后把当月攒的钱归档到存款明细（提示文案如「9000 元，攒钱，2026.9 月收入已归档」）。

页面主体（自上而下）：
1. 月度总览像素卡片：四栏像素数字，「月度总花销 ¥X,XXX」「总支出 ¥X,XXX」
   「支出最多日 9月18日」「花销最多日 9月18日」，每栏配像素小图标。
2. 分类占比环形图像素卡片：标题「各类花销占比」，
   圆形像素图展示餐饮/交通/购物等各类占比，不同类别不同颜色，
   点击某个比例区域后，卡片下方显示该类的具体金额（如「餐饮 ¥1,234.00」）。

页面底部：一行像素小字「数据每日自动同步生成，不可编辑」。

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 七、年度总结页提示词

```
竖版手机记账 App 年度总结页，星露谷式像素马赛克风格，米白像素纹理背景，
深棕像素描边，阶梯硬阴影，像素字体，低饱和暖色，无卡通吉祥物。

页面顶部：居中是像素大字标题「2026年 年度总结」，标题旁带下拉箭头（默认收缩）。

页面主体（自上而下）：
1. 年度总览像素卡片：四栏像素数字，「年度总花销 ¥XX,XXX」「总支出 ¥XX,XXX」
   「支出最多月 9月」「花销最多月 9月」，「9月」以像素按钮样式呈现、可点击跳转到该月
   月度总结页。
2. 分类占比环形图像素卡片：标题「全年各类花销占比」，
   圆形像素图展示餐饮/交通/购物等全年占比，不同类别不同颜色，
   点击某个比例区域后，卡片下方显示该类的具体金额（如「餐饮 ¥12,345.00」）。

页面底部：一行像素小字「数据每日自动同步生成，不可编辑」。

严格遵守全局 UI 规范：同类型图标一致、同级字号一致、元素严格对齐、不省略文字。
```

---

## 八、图标设计提示词

**App 启动图标**

```
记账 App 启动图标，星露谷式低分辨率像素马赛克，米白像素纹理背景：
主体为一个像素画的木色账本，封面上有像素金币和像素加号，
账本下方是像素草地色块，整体色块感强烈、锯齿边缘、深棕像素描边、
阶梯硬阴影，配色奶油白、草绿、木棕、陶土橘，无手绘平滑线条，居中构图。
```

**分类图标系列（像素画，32x32，统一风格）**

```
一套记账 App 分类图标，全部为 32x32 低分辨率像素画、色块感强烈、
深棕像素描边、像素高光与抖动渐变、同一套像素阴影，米白背景：
1. 餐饮：像素汉堡
2. 交通：像素小汽车
3. 购物：像素购物袋
4. 娱乐：像素游戏手柄
5. 居住：像素小房子
6. 工资：像素一叠钞票
7. 理财：像素金币
8. 医疗：像素小药箱
9. 其他：像素省略号
10. 收入类：像素向上的箭头 + 金币
11. 金钱类：像素金币堆
12. 非金钱类：像素礼盒
13. 天气：像素太阳（晴）、像素云朵（多云）、像素雨滴（雨）、像素雪花（雪）
全套图标风格、像素密度、描边、光照完全统一。
```

---

## 九、使用说明

| 用途 | 用哪段 | 喂给谁 |
|---|---|---|
| 生成整个 App | 「一、项目总览主提示词」 | App 生成类 AI（可交互网页） |
| 单个页面细化 | 「二 ~ 七」按需取 | 图像生成 / UI 生成类 AI |
| 图标与天气 | 「八、图标设计」 | 图像生成类 AI（1:1） |
| 全局约束 | 「〇、全局 UI 硬性规范」 | 加在任何提示词开头 |
